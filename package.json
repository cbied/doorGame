{
  "name": "doorgame",
  "version": "1.0.0",
  "description": "door game, avoid the choreBot",
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rimraf dist && rimraf es5File.js",
    "js": "babel file.js --out-file es5File.js",
    "usemin": "usemin index.html -d dist --htmlmin -o dist/index.html",
    "build": "npm run clean && npm run js && npm run usemin"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cbied/doorGame.git"
  },
  "author": "C. Biediger",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/cbied/doorGame/issues"
  },
  "homepage": "https://github.com/cbied/doorGame#readme",
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "cssmin": "^0.4.3",
    "htmlmin": "0.0.7",
    "rimraf": "^2.6.2",
    "uglifyjs": "^2.4.11",
    "usemin-cli": "^0.5.1"
  }
}
